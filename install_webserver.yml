---
- name: install web server
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      apt:
        name: nginx
        state: latest
        update_cache: True
      notify:
        - Start Nginx
        
  handlers:
    - name: Start Nginx
      Service: 
        name: nginx
        state: started
