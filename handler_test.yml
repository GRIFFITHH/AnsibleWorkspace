---
- name: handler test
  hosts: 192.168.0.108
  handlers:
    - name: stop message
      debug:
        msg: stop play
        
        
  tasks:
    - name: excute ls
      shell: ls
      notify: 
        - stop message
        
    - name: status sshd
      shell: systemctl status sshd
      notify:
        - stop message
