---
- name: handler test
  hosts: 192.168.0.108
  handlers:
    - name: stop message
      debug:
        msg: " stop {{ taskname }} "
        
        
  tasks:
    - name: excute ls
      shell: ls
      var:
        taskname: ls
      notify: 
        - stop message
        
    - name: status sshd
      shell: systemctl status sshd
      var:
        taskname: sshd
      notify:
        - stop message
